# Parser and fuzz regression corpus

Every minimized parser or property-test failure belongs here as a named,
permanent pytest case. Include the originating workflow run in a comment so a
future parser upgrade can be reviewed as a security-sensitive change.
